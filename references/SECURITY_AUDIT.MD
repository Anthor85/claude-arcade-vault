# Auditoría de seguridad

Memoria del agente `security-guardian`. Referencia: SPEC 13, SPEC 15 y
`references/security/security-checklist.md`.

## Histórico de auditorías

| Fecha | Veredicto general | Cambios desde la anterior |
| ----- | ----------------- | ------------------------- |
| 2026-08-26 (auth, deep-dive) | Sin cambios respecto a la auditoría previa del mismo día | Revisión enfocada en autenticación (login/registro, sesión, cookies, proxy, RLS de auth) a petición explícita, sin recorrer todo el checklist general. Confirmado: `getUser()` (no `getSession()`) en `lib/supabase/session.ts` y `proxy.ts`; contraseña nunca se hashea en la app (delegado a Supabase Auth); `PASSWORD_RE` solo en `signUp` (decisión de SPEC 15, no en `signIn`); cookies de sesión gestionadas por `@supabase/ssr` sin overrides propios (httpOnly/secure/sameSite por defecto del SDK); Server Actions de Next.js llevan protección CSRF nativa (comprobación de `Origin`), no hay formularios GET mutando estado; ninguna referencia a `service_role` en el repo, solo claves publishable/anon; `signIn` devuelve el mismo mensaje genérico ("CORREO O CONTRASEÑA INCORRECTOS") tanto si el email no existe como si la contraseña es incorrecta (comentario explícito en el código lo confirma). `get_advisors` (security) repite los mismos 3 lints ya conocidos: `rls_auto_enable()` (riesgo aceptado) y `auth_leaked_password_protection` (WARN, sigue sin activar en panel, mismo hallazgo pendiente que la auditoría anterior). RLS confirmado activo (`rowsecurity = true`) en `profiles` y `scores`, con las 4 políticas exactas de SPEC 04 (`SELECT` público en ambas, `UPDATE` propio en `profiles`, `INSERT` propio en `scores`, sin políticas de `DELETE`/otro `UPDATE`/`INSERT` en `profiles` más allá del trigger). `handle_new_user()` sigue sin `EXECUTE` para `anon`/`authenticated` (verificado con `has_function_privilege`), incluida la versión que añade el fallback de `username` para OAuth (SPEC 13). Único punto no cubierto por SPEC 13/15 pero preguntado explícitamente: el checklist solo documenta el límite de *signup* por IP, no un rate-limit de *intentos de login*; eso vive en los valores por defecto de Supabase Auth y no está descrito en ninguna spec — se deja como observación informativa, no como hallazgo, al no estar en el alcance de SPEC 13/15/checklist. |
| 2026-08-26 | Aceptable, con un hallazgo pendiente | Primera auditoría registrada. `get_advisors` (security) muestra 3 lints: los 2 esperados de `rls_auto_enable()` (riesgo aceptado) y `auth_leaked_password_protection` (WARN) todavía activo — SPEC 15 documentaba este ajuste como paso operativo pendiente de aplicar en el panel y su criterio de aceptación ("`get_advisors` ya no reporta `auth_leaked_password_protection`") no se cumple todavía. Headers de `next.config.ts`, `PASSWORD_RE`/`USERNAME_RE` de `app/acceso/actions.ts`, pista de cliente en `auth-form.tsx`, `proxy.ts` (solo refresco + redirect en `/acceso`) y callback OAuth (`app/auth/callback/route.ts`) verificados contra SPEC 13/15, todos conformes. RLS activo y con las políticas esperadas en `profiles` y `scores`. `handle_new_user()` sigue con `EXECUTE` revocado de `anon`/`authenticated`/`public` tras la migración de SPEC 13 (`20260826070434_username_oauth_fallback`), que la recrea sin restaurar esos grants. |

## Riesgos aceptados (no perseguir)

| Riesgo                                                           | Origen  | Decisión                                                |
| ---------------------------------------------------------------- | ------- | ------------------------------------------------------- |
| `public.rls_auto_enable()` ejecutable por `anon`/`authenticated` | SPEC 15 | Aceptado explícitamente por el usuario; no se persigue. |

## Hallazgos nuevos pendientes de spec

- **`auth_leaked_password_protection` sigue en WARN (2026-08-26).** SPEC 15 lo trataba como paso operativo de panel (Authentication → Policies) y su criterio de aceptación exigía que `get_advisors` dejara de reportarlo; a día de hoy sigue apareciendo, es decir, o no se activó en el panel o se desactivó después. No es un riesgo aceptado (SPEC 15 no lo marca como tal, al contrario que `rls_auto_enable()`): requiere revisar el panel de Supabase (Authentication → Policies → Leaked password protection) y, si sigue sin poder activarse, valorar una spec de seguimiento.
